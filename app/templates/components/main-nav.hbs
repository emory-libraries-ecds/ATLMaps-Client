{{!-- <div class="navigation-wrapper">
    {{#link-to 'flat-page' class='logo' }}
        <img src="/assets/images/atl.png" alt="Logo Image">
    {{/link-to}}
    <a href="javascript:void(0)" class="navigation-menu-button" id="js-mobile-menu">MENU</a>
    <nav role="navigation">
        <ul id="js-navigation-menu" class="navigation-menu show">
            {{#link-to 'flat-page'}}
              <li class='nav-link title'>
                <span style='font-weight:900'>ATL</span>Maps
              </li>
            {{/link-to}}
              <li class='nav-link project-title'>
                  {{#link-to 'flat-page.projects'}}Projects{{/link-to}}
              </li>
              <li class='nav-link project-title'>
                  {{#link-to 'explore'}}Explore{{/link-to}}
              </li>
              <li class='nav-link'>
                  {{#link-to 'flat-page.about'}}About{{/link-to}}
              </li>
        </ul>
    </nav>
    <div class="navigation-tools">
        {{#if session.isAuthenticated}}
        {{#basic-dropdown as |dd|}}
          {{#dd.trigger class="trigger-bootstrap-feel"}}<i class="fa fa-user" aria-hidden="true"></i> Account{{/dd.trigger}}

          {{#dd.content class="content-bootstrap-feel z-depth-3"}}
          <ul class="account-fa-ul fa-ul title-bar-action">
            <li onclick={{action 'invalidateSession'}}>
              <i class='fa-li fa fa-sign-out' aria-hidden='true'></i> Logout
            </li>
            {{#if currentUser.user.confirmed}}
            <li>{{#ember-inline-edit
              value=currentUser.user.displayname
              onSave=(action 'updateDisplayName' currentUser.user)
              onCancel=(action 'cancelUpdate' currentUser.user)
              as |inline-edit|}}
              {{#inline-edit.editable}}
                <span><i>{{currentUser.user.displayname}}</i> <i class="fa-li fa fa-pencil-square" aria-hidden="true"></i></span>
              {{/inline-edit.editable}}

              {{#inline-edit.editor}}
                {{input value=currentUser.user.displayname}}
              {{/inline-edit.editor}}
            {{/ember-inline-edit}}
            </li>
            {{else}}
            <p>Your account has not been confirmed. Please check your email.</p>
            {{/if}}
          </ul>
          {{/dd.content}}
        {{/basic-dropdown}}
        {{else}}
        {{#login-link}}<i class="fa fa-sign-in" aria-hidden="true"></i> Log in{{/login-link}}
        {{/if}}
    </div>
</div>
{{#if (or model.project.may_edit model.project.exploring)}}
<div class='search-head {{if model.project.showingSearch 'showing-search-head' 'collapse-head'}}'>
    <div class='search-head-title' onclick={{action (toggle-action 'showingSearch' model.project)}}>
        <i class="fa fa-chevron-left" aria-hidden="true"></i><i class="fa fa-chevron-left" aria-hidden="true"></i> Find Layers
    </div>
</div>
{{/if}}
--}}
<nav class="uk-navbar-container uk-navbar-transparent" uk-navbar>
    <div class="uk-navbar-left">

        <ul class="uk-navbar-nav">
            <li>{{#link-to 'flat-page' class='logo' }}
                <img src="/assets/images/atl.png" alt="Logo Image"> <span style='font-weight:900'>ATL</span>Maps
                {{/link-to}}
            </li>
            <li>
                {{#link-to 'flat-page.projects'}}Projects{{/link-to}}
            </li>
            <li>
                {{#link-to 'flat-page.projects'}}Projects{{/link-to}}
            </li>
            <li>
                {{#link-to 'explore'}}Explore{{/link-to}}
            </li>
            <li>
                {{#link-to 'flat-page.about'}}About{{/link-to}}
            </li>
        </ul>

    </div>
    <div class="uk-navbar-right">

        <ul class="uk-navbar-nav">
            <li>
                <a href="#"><i class="fa fa-user" aria-hidden="true"></i> Account</a>
                <div class="uk-navbar-dropdown">
                    <ul class="uk-nav uk-navbar-dropdown-nav">
                    {{#if session.isAuthenticated}}
                        <li>
                            <a href='{{action 'invalidateSession'}}'>
                                <i class='fa-li fa fa-sign-out' aria-hidden='true'></i> Logout
                            </a>
                        </li>
                        {{#if currentUser.user.confirmed}}
                            <li>
                                {{currentUser.user.displayname}}
                                {{!-- {{#ember-inline-edit
                                    value=currentUser.user.displayname
                                    onSave=(action 'updateDisplayName' currentUser.user)
                                    onCancel=(action 'cancelUpdate' currentUser.user)
                                    as |inline-edit|}}
                                    {{#inline-edit.editable}}
                                        <span><i>{{currentUser.user.displayname}}</i> <i class="fa-li fa fa-pencil-square" aria-hidden="true"></i></span>
                                    {{/inline-edit.editable}}

                                    {{#inline-edit.editor}}
                                        {{input value=currentUser.user.displayname}}
                                    {{/inline-edit.editor}}
                                {{/ember-inline-edit}} --}}
                            </li>
                        {{else}}
                            <li>Your account has not been confirmed. Please check your email.</li>
                        {{/if}}
                    {{else}}
                        <li>
                            {{#login-link}}<i class="fa fa-sign-in" aria-hidden="true"></i> Log in{{/login-link}}
                        </li>
                    {{/if}}
                    </ul>
                </div>
            </li>
        </ul>
    </div>
</nav>
{{!-- empty div is the wormhole --}}
<div id='results' class='search-pane {{if model.project.showingSearch 'showing-search' 'collapse-pane'}}' />
