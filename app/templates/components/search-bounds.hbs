{{!-- <a {{action 'searchBounds'}} class={{if hasBounds 'has-bounds'}}>
    <i class="fa {{if hasBounds 'has-bounds fa-check-square-o' 'fa-square-o'}}" aria-hidden="true"></i>
</a> Search this area --}}

{{input type='checkbox' id='toggle-bounds' change=(action 'searchBounds')}} <label for='toggle-bounds'>Search this area</label>

{{#if this.moved}}
<button {{action 'updateBounds'}} class='update-bounds'>Search this area</button>
{{/if}}
